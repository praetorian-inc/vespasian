name: Leaderboard Metrics

on:
  pull_request_target:
    types: [closed]

permissions:
  contents: read
  pull-requests: read
  id-token: write

jobs:
  metrics:
    uses: praetorian-inc/public-workflows/.github/workflows/leaderboard-metrics.yml@2cce562404572f0e8004fc0fa1ad91a62a41592f # v2.19.0
    with:
      capability_map: '[{"path": ".", "name": "vespasian"}]'