// Sample JavaScript with API endpoints
fetch('/api/users');
fetch('/api/posts/' + postId);

const xhr = new XMLHttpRequest();
xhr.open('GET', '/api/data');

axios.post('/api/submit', data);

const API_BASE = '/api/v1';

// Template literal
fetch(`/api/resource/${id}`);
