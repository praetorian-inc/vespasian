{
  "description": "Expected API paths from a live crawl of the REST API target. The crawler discovers GET endpoints via links on the index page and POST endpoints via JS fetch calls. /api/subscribe is discovered via generate's analyze.ExtractForms augmentation from the static HTML <form> on the index page (LAB-3890 T2). Parameter names may vary (e.g., {userId} vs {id}).",
  "paths": {
    "/api/health": ["GET"],
    "/api/users": ["GET"],
    "/api/users/{id}": ["GET"],
    "/api/users/{id}/email-address": ["GET"],
    "/api/products": ["GET"],
    "/api/products/{id}": ["GET"],
    "/api/orders": ["GET"],
    "/api/orders/{id}": ["GET"],
    "/api/login": ["POST"],
    "/api/upload": ["POST"],
    "/api/subscribe": ["POST"]
  },
  "total_paths": 11,
  "excluded_patterns": [".js", ".css", ".png", ".ico", ".svg", ".woff"]
}
